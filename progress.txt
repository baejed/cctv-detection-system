## Onboarding & Visualization — Progress

Source: docs/onboarding-issues.md

### Issue #1 — Wizard shell + "Get Started" entry + per-user progress ✅ DONE (prior session)

- OnboardingWizard.tsx: full-page takeover with 7 steps, step indicators, back/next nav
- "Get Started" in Layout sidebar (Rocket icon, always visible)
- "Get Started" on empty Intersections empty-state (onOpenWizard from outlet context)
- server/routers/onboarding.py: GET/PATCH /onboarding/progress per-user
- alembic/versions/0015_user_wizard_step.py: wizard_step column on users table
- eyegila/src/services/onboarding.ts: getProgress / setProgress

### Issue #7 — Warrant evidence 24-hour bar chart ✅ DONE (2026-06-15)
Commit: db4d0a5

- eyegila/src/components/recommendations/LatestTab.tsx:
  - buildHourlyBuckets(): groups AggregationRow[] by hour into vehicleCounts + pedCounts arrays
  - WarrantChart component: Recharts BarChart with Cell-colored bars (emerald = qualifying,
    muted = non-qualifying), ReferenceLine at MUTCD threshold, qualifying hrs counter
  - useEffect fetches last 24h hourly aggregation data alongside existing health check
  - W1 vehicle chart (≥400 veh/hr, 8 qualifying hours target) always shown
  - W4 pedestrian chart (≥100 ped/hr) shown only when pedestrian data present
  - Empty-state fallback: dashed box "No detection data for last 24 hours"
  - Existing probability bars + threshold table retained below chart

### Issue #3 — ONVIF discovery + camera grouping + direction assignment ✅ DONE (2026-06-15)

- eyegila/src/components/OnboardingWizard.tsx:
  - Replaced placeholder stubs for discover/name/assign steps with real working UI
  - discover step: ONVIF scan button, NVR/DVR form (collapsible), manual RTSP URL entry,
    camera checklist with inline RTSP edit; existing cameras filtered out via cctvsApi.list()
  - name step: intersection name input + Leaflet map click-to-pin (same map as IntersectionSetupWizard)
  - assign step: per-camera name + direction (N/S/E/W) selects with default direction order
  - handleNext() validates per step (≥1 camera, non-empty name) before advancing
  - On assign → regions: creates intersection, streets (one per unique direction), cameras via API;
    stores createdIntersectionId for downstream steps; shows toast on success
  - creating spinner shown in footer Next button while API calls in flight
  - Existing IntersectionSetupWizard modal is still available for direct use elsewhere

### Issue #4 — Draw regions step (guided overlay) ✅ DONE (2026-06-15)
Commit: 4f705fe

- eyegila/src/components/OnboardingWizard.tsx:
  - 'regions' step now renders as a compact floating guide panel (fixed top-4 right-4 z-50 w-80)
    instead of the full-screen takeover, so CameraDetail is accessible beneath it
  - loadRegionStatus(intersectionId): fetches cctvsApi.list() + /regions/, cross-references
    to produce {id, name, hasRegions}[] for every camera in the intersection
  - Entering 'regions' step: auto-resolves target intersection — uses createdIntersectionId
    from the wizard session, or falls back to the most-recently created intersection when
    the wizard is resumed after a close
  - Each camera row shows ✓/○ status badge + "Open ↗" link to /cameras/:id
  - Refresh button re-calls loadRegionStatus() without leaving the current page
  - Next button is disabled until regionCams.every(c => c.hasRegions); green banner
    "All cameras have regions — ready to continue!" shown when satisfied
  - Back → returns to 'assign' step (full-screen wizard); Next → advances to 'timing'

### Issue #5 — Enter current timing + live phase diagram ✅ DONE (2026-06-15)
Commit: 4a147a8

- eyegila/src/components/OnboardingWizard.tsx:
  - Timing step state: timingTargetId, timingApproaches, timingStatus, timingCycle,
    timingGreenSplits, timingLoading
  - useEffect on entering 'timing' step: resolves intersection (createdIntersectionId or
    most-recent), fetches intersection + streets, derives approach directions (excluding
    'unknown'), pre-fills existing timing data if present, defaults green splits to
    equal division of cycle length
  - handleNext() 'timing' branch: PATCHes /intersections/{id}/timing with signal_status,
    existing_cycle_length, existing_green_splits (keyed by arm_direction string)
  - UI: signal status toggle (unsignalized / fixed_time / actuated); cycle length input;
    per-approach green-seconds table with live % column; CSS Gantt phase diagram —
    proportional green/yellow(3s)/red bars per approach, updates on every keystroke;
    legend row; loading spinner while fetching

### Issue #6 — Collecting data waiting state + sidebar progress indicator ✅ DONE (2026-06-15)
Commit: 38cbf62

- eyegila/src/components/OnboardingWizard.tsx:
  - Added collecting step state: collectingTargetId, collectingHasRec,
    collectingLastDetection, collectingLoading
  - useEffect on entering 'collecting' step: resolves intersection (same fallback pattern
    as regions/timing), fetches recommendationsApi.list() + dataHealth() in parallel
  - Two UI states:
    - "Results ready" (collectingHasRec = true): emerald banner with BarChart3 icon and
      "View results on Intersections page" button that closes wizard + navigates to /
    - "Still collecting" (default): animated Wifi icon, last-detection timestamp or
      "no detections yet" message, "Refresh status" button, "Go to Intersections" shortcut
  - Close wizard and go to app is prominently supported in both states

- eyegila/src/components/Layout.tsx:
  - Added intersectionsApi import + Intersection type
  - intersectionList state: fetched on mount and whenever wizard closes (handleWizardClose)
  - Sidebar "Setup Progress" widget (hidden in icon-collapsed mode):
    - Shows "X/Y" configured count; "configured" = existing_cycle_length is set
    - Proportional progress bar (emerald at 100%, primary otherwise)
    - Subtitle: "All intersections configured" or "N pending timing setup"

### Issue #8 — Timing comparison visualization ✅ DONE (2026-06-15)
Commit: 069a26d

- eyegila/src/pages/SignalTiming.tsx:
  - ARM_SHORT constant: direction → single-letter abbreviation (N/S/E/W)
  - YELLOW_S = 3: fixed intergreen constant shared across both gantt components
  - GanttBar component: single approach row — label + stacked green/yellow/red bar;
    green and red seconds rendered inline when bar is wide enough; full value on hover title
  - GanttDiagram component: title + cycle length header + stack of GanttBars
  - Phase comparison section (after per-chunk table, before queue chart):
    - Left panel: "Current timing" using intersection.existing_cycle_length +
      existing_green_splits (keyed by arm_direction); falls back to dashed placeholder
      prompting wizard entry when no timing has been saved yet
    - Right panel: "Recommended (Webster)" in emerald using activeTiming.cycle_length +
      green_splits (keyed by street_id string); always shown when activeTiming exists
    - Divider between panels hidden on mobile (sm:block)
    - Shared legend strip (Green / Yellow 3s / Red) at bottom
    - Updates automatically when a different time-of-day chunk is clicked in the table above

### Issue #9 — Simulation queue animation + council export ✅ DONE (2026-06-15)
Commit: f0f5e4b

- eyegila/src/pages/SignalTiming.tsx:
  - presentMode state + Escape key handler (closes overlay on Esc)
  - "Present" button (MonitorPlay icon) added to simulation section controls
  - Fixed inset-0 z-[100] overlay: dark bg (#0a0f1a), no nav/sidebar visible
  - Overlay header: intersection name, chunk period, vehicle_hours_saved (emerald)
  - Minimal controls bar: 2D/3D toggle, SBS toggle, before/after toggle, play/pause, speed
  - Overlay body: DualIntersectionCanvas (2D) or IntersectionScene3D(s) (3D single/SBS)
    — shares view3D/sbs3D/paused3D/speed3D state with the regular section
  - 2D side-by-side (before/after), 3D SBS, paused, scrub/reset all work from within
    present mode; vehicle_hours_saved shown in the header strip
  - All acceptance criteria met: 2D SBS animation ✓, 3D comparison ✓, chrome-free
    screenshot mode ✓, pause/replay ✓, vehicle_hours_saved below animation ✓

    - Updates automatically when a different time-of-day chunk is clicked in the table above
