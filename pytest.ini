[pytest]
testpaths = tests
addopts = -v --tb=short -m "not ratelimit"
pythonpath = .
markers =
    ratelimit: marks tests that exhaust the login rate limit (deselected by default, run with -m ratelimit)
